# 更新日志

## [1.1.6] - 2026-09-02 · 修订 3

### 新增

- **通用扫码点位**：新增/编辑手动文本点位时可勾选「支持 USB 扫码枪输入」
- **Windows 后台采集服务**：安装包可选安装并启动 `HuaGuang.Monitor.Service`
- **运行状态 Int16**：0=停止、1=运行、2=待机；Excel 支持增量补丁（保留用户自定义地址）

### 改进

- 默认精度改为 2 位小数；设置项「温度精度」更名为「精度」
- 历史页表格横向滚动；点位编辑页增加「返回」
- 删除点位时避免分组列表崩溃；监控页扫码面板通用化

## [1.1.5] - 2026-08-25 · 修订 2

### 改进

- **MQTT 异步发送**：采集读点与 MQTT 发布解耦，Broker 不可达时不阻塞刷新；失败消息入队缓存（最多 128 条）
- **Android 轮询**：专用采集线程 + 单调时钟；MQTT 连接 5 秒硬超时
- **Modbus 批量读取**：合并请求、单次超时、失败快速中止
- **模拟数据**：不连接 PLC；产品货号扫码输入改为点击后弹出

## [1.1.4] - 2026-08-24 · 修订 1

### 新增

- **产品货号**：默认点表手动输入，MQTT 字段 `cphh`；监控页支持 Mexxen 等 USB 扫码枪（键盘模式）
- **历史自定义时间**：可按开始/结束时间筛选，支持只设一端

### 改进

- 启动默认全屏；卸载时可选择是否删除用户数据（`LocalAppData`）
- 默认轮询周期 60s；产线 Excel 升级时自动补全新点位与 MQTT 映射
- 修复 `Get-AppVersion.ps1` 修订号解析；Inno Setup 版本常量嵌套问题

## [1.1.3] - 2026-08-22 · 修订 3

### 新增

- **历史页表格视图**：等宽列展示时间、设备与各点位值；分页加载，底部「加载更多」
- **历史数据删除**：单条删除、按当前筛选删除、清空全部

### 改进

- **历史页**：进入页面不自动加载，仅手动点「刷新」；修复列表无法下滑与加载更多中断问题
- **点位页**：改用虚拟化分组 `CollectionView`，减少卡顿；数据未变时不重复重建
- **监控页**：竖屏顶栏三行布局，横屏保持单行；竖屏按钮区可横向滚动
- **设置持久化**：重启后保留 `settings.json` 修改，Excel 仅在首次安装或点表为空时覆盖
- 设置页保存时允许采集运行中写入（带提示）

## [1.1.2] - 2026-08-21

### 新增

- **产线 Excel 配置**：每条产线一个 xlsx（配置、MQTT 报文、字段映射、点表、显示分组）
- **本地历史记录**：SQLite 存储采集/订阅快照，可配置开关与保留天数
- **MQTT 扩展属性**：ClientId、用户名、密码、TLS 等连接参数

### 改进

- 点表「显示分组」列控制监控页分组与卡片颜色
- 打包与图标生成相关修复

## [1.1.1] - 2026-08-20

### 新增

- **诊断 Tab**：软件自检，运行核心单元测试（Modbus、MQTT、Excel、历史存储等）

### 改进

- 修复桌面端与订阅模式 UI 问题
- 降低长时间运行内存占用

## [1.1.0] - 2026-08-20

### 新增

- **订阅模式**：连接 MQTT Broker，查看局域网内其他设备的遥测
- **多主题订阅**：配置多个 MQTT 主题，首页「全部 / 单主题」随时切换
- **运行中添加主题**：订阅进行中可直接添加主题并自动重连
- **Windows 安装包**：Inno Setup 生成 `IndustrialMonitor-Setup.exe`，VS Release 发布自动打包
- **开机自动启动**与**启动后自动采集/订阅**
- **点位显示精度**：各 tag 可单独设置 0–4 位小数
- 默认点表增加手动标签：胶辊型号、胶水型号、门幅、厚度

### 改进

- 共享 `MqttConnectionFactory`，采集与订阅复用连接配置
- 设置页运行模式切换；订阅主题列表增删管理
- 开发脚本：编译、发布、安装包、Android SDK、MQTT 测试

## [1.0.0] - 2026-08-19

- 初版：信捷 XD5E Modbus TCP 采集、MQTT JSON 发布、三 Tab MAUI 界面
